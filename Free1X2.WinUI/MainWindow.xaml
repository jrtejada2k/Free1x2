<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Free1X2.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls">

    <Grid Background="{ThemeResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Barra de título custom (drag region) -->
        <Border x:Name="AppTitleBar" Grid.Row="0" Height="36">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0" Spacing="8">
                <FontIcon Glyph="&#xE9D9;" FontSize="14" Foreground="{ThemeResource AppAccentBrush}" />
                <TextBlock Text="Free1X2" VerticalAlignment="Center"
                           Style="{StaticResource CaptionTextBlockStyle}" />
            </StackPanel>
        </Border>

        <!-- Barra de menús (organización del programa original) -->
        <muxc:MenuBar x:Name="BarraMenu" Grid.Row="1" />

        <!-- Contenido -->
        <Frame x:Name="ContentFrame" Grid.Row="2" />
    </Grid>
</Window>
