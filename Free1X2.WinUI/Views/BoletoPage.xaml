<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Free1X2.WinUI.Views.BoletoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="32" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Boleto base" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock x:Name="ResumenTexto" Text="Fijos: 0 · Dobles: 0 · Triples: 0"
                       Foreground="{ThemeResource AppTextSecondaryBrush}" />
        </StackPanel>

        <Border Grid.Row="2" Background="{ThemeResource AppSurfaceBrush}"
                BorderBrush="{ThemeResource AppBorderBrush}" BorderThickness="1"
                CornerRadius="{StaticResource AppCardCornerRadius}" Padding="12" MaxWidth="520"
                HorizontalAlignment="Left" VerticalAlignment="Top">
            <ScrollViewer>
                <StackPanel x:Name="PartidosPanel" Spacing="6" />
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
