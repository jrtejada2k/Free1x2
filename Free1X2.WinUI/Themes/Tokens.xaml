<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:media="using:Microsoft.UI.Xaml.Media">

    <!-- ============================================================
         Sistema de diseño Free1X2 — paleta Slate / Indigo
         Mapeo de los tokens de ModernTheme.cs a recursos WinUI 3.
         ============================================================ -->
    <ResourceDictionary.ThemeDictionaries>

        <!-- ===== LIGHT (paleta "Índigo & Teal" elegida por el usuario) ===== -->
        <ResourceDictionary x:Key="Light">
            <!-- Acento índigo (barra de herramientas, título, botón Calcular, selección) -->
            <Color x:Key="SystemAccentColor">#3D5A80</Color>
            <Color x:Key="SystemAccentColorLight1">#4E6E97</Color><!-- índigo +claro (derivado) -->
            <Color x:Key="SystemAccentColorLight2">#6B89AE</Color><!-- índigo ++claro (derivado) -->
            <Color x:Key="SystemAccentColorDark1">#314866</Color><!-- índigo +oscuro (derivado) -->
            <Color x:Key="SystemAccentColorDark2">#25374D</Color><!-- índigo ++oscuro (derivado) -->

            <Color x:Key="AppBackground">#F4F6F8</Color><!-- gris azulado claro -->
            <Color x:Key="AppSurface">#FFFFFF</Color>
            <Color x:Key="AppSurfaceAlt">#E6ECF2</Color><!-- índigo pálido (paneles/cabeceras, derivado) -->
            <Color x:Key="AppBorder">#C9D4DF</Color><!-- índigo-gris claro (derivado) -->
            <Color x:Key="AppText">#293241</Color>
            <Color x:Key="AppTextSecondary">#5C6B7A</Color><!-- pizarra apagada (derivado de #293241) -->
            <Color x:Key="AppSuccess">#2E7D32</Color><!-- verde (condición/signo activo) -->
            <Color x:Key="AppWarning">#EE6C4D</Color><!-- coral (acento terciario / advertencia) -->
            <Color x:Key="AppOnSuccess">#FFFFFF</Color>
            <Color x:Key="AppError">#C0392B</Color>

            <!-- Acentos secundario (teal) y terciario "detalle" (coral) -->
            <Color x:Key="AppSecondary">#2A9D8F</Color><!-- teal (cabeceras de sección, resaltes) -->
            <Color x:Key="AppTertiary">#EE6C4D</Color><!-- coral (acentos pequeños) -->

            <!-- Signos 1/X/2 marcados en verde, como el boleto del original -->
            <SolidColorBrush x:Key="ToggleButtonBackgroundChecked"            Color="#2E7D32" />
            <SolidColorBrush x:Key="ToggleButtonBackgroundCheckedPointerOver" Color="#357A38" />
            <SolidColorBrush x:Key="ToggleButtonBackgroundCheckedPressed"     Color="#27692B" />
            <SolidColorBrush x:Key="ToggleButtonForegroundChecked"            Color="#FFFFFF" />
            <SolidColorBrush x:Key="ToggleButtonForegroundCheckedPointerOver" Color="#FFFFFF" />
            <SolidColorBrush x:Key="ToggleButtonForegroundCheckedPressed"     Color="#FFFFFF" />

            <SolidColorBrush x:Key="AppBackgroundBrush"     Color="{StaticResource AppBackground}" />
            <SolidColorBrush x:Key="AppSurfaceBrush"        Color="{StaticResource AppSurface}" />
            <SolidColorBrush x:Key="AppSurfaceAltBrush"     Color="{StaticResource AppSurfaceAlt}" />
            <SolidColorBrush x:Key="AppBorderBrush"         Color="{StaticResource AppBorder}" />
            <SolidColorBrush x:Key="AppTextBrush"           Color="{StaticResource AppText}" />
            <SolidColorBrush x:Key="AppTextSecondaryBrush"  Color="{StaticResource AppTextSecondary}" />
            <SolidColorBrush x:Key="AppSuccessBrush"        Color="{StaticResource AppSuccess}" />
            <SolidColorBrush x:Key="AppWarningBrush"        Color="{StaticResource AppWarning}" />
            <SolidColorBrush x:Key="AppErrorBrush"          Color="{StaticResource AppError}" />
            <SolidColorBrush x:Key="AppAccentBrush"         Color="{StaticResource SystemAccentColor}" />
            <SolidColorBrush x:Key="AppSecondaryBrush"      Color="{StaticResource AppSecondary}" />
            <SolidColorBrush x:Key="AppTertiaryBrush"       Color="{StaticResource AppTertiary}" />
        </ResourceDictionary>

        <!-- ===== DARK ===== -->
        <ResourceDictionary x:Key="Dark">
            <Color x:Key="SystemAccentColor">#818CF8</Color>
            <Color x:Key="SystemAccentColorLight1">#A5B4FC</Color>
            <Color x:Key="SystemAccentColorLight2">#C7D2FE</Color>
            <Color x:Key="SystemAccentColorDark1">#6366F1</Color>
            <Color x:Key="SystemAccentColorDark2">#4F46E5</Color>

            <Color x:Key="AppBackground">#0B1120</Color>
            <Color x:Key="AppSurface">#0F172A</Color>
            <Color x:Key="AppSurfaceAlt">#1E293B</Color>
            <Color x:Key="AppBorder">#334155</Color>
            <Color x:Key="AppText">#F1F5F9</Color>
            <Color x:Key="AppTextSecondary">#94A3B8</Color>
            <Color x:Key="AppSuccess">#16A34A</Color><!-- green-600 (dark): mejor contraste con texto blanco -->
            <Color x:Key="AppWarning">#F59E0B</Color>
            <Color x:Key="AppOnSuccess">#FFFFFF</Color>
            <Color x:Key="AppError">#EF4444</Color>

            <!-- Acentos secundario (teal) y terciario (coral) — paralelos al Light -->
            <Color x:Key="AppSecondary">#2DD4BF</Color>
            <Color x:Key="AppTertiary">#FB7185</Color>

            <SolidColorBrush x:Key="AppBackgroundBrush"     Color="{StaticResource AppBackground}" />
            <SolidColorBrush x:Key="AppSurfaceBrush"        Color="{StaticResource AppSurface}" />
            <SolidColorBrush x:Key="AppSurfaceAltBrush"     Color="{StaticResource AppSurfaceAlt}" />
            <SolidColorBrush x:Key="AppBorderBrush"         Color="{StaticResource AppBorder}" />
            <SolidColorBrush x:Key="AppTextBrush"           Color="{StaticResource AppText}" />
            <SolidColorBrush x:Key="AppTextSecondaryBrush"  Color="{StaticResource AppTextSecondary}" />
            <SolidColorBrush x:Key="AppSuccessBrush"        Color="{StaticResource AppSuccess}" />
            <SolidColorBrush x:Key="AppWarningBrush"        Color="{StaticResource AppWarning}" />
            <SolidColorBrush x:Key="AppErrorBrush"          Color="{StaticResource AppError}" />
            <SolidColorBrush x:Key="AppAccentBrush"         Color="{StaticResource SystemAccentColor}" />
            <SolidColorBrush x:Key="AppSecondaryBrush"      Color="{StaticResource AppSecondary}" />
            <SolidColorBrush x:Key="AppTertiaryBrush"       Color="{StaticResource AppTertiary}" />
        </ResourceDictionary>

    </ResourceDictionary.ThemeDictionaries>

    <!-- ===== Tipografía base (app-wide) =====
         Segoe UI Variable es la fuente de sistema moderna de Windows 11:
         limpia y profesional, sin nada decorativo. Se declara con fallback a
         "Segoe UI" por si la variante variable no está disponible.
         Al sobreescribir ContentControlThemeFontFamily (recurso del framework
         del que heredan los estilos base de texto y la mayoría de controles),
         toda la app adopta la fuente sin tocar cada control. Sólo cambia la
         FAMILIA de fuente; tamaños, color y disposición se conservan. -->
    <FontFamily x:Key="AppFontFamily">Segoe UI Variable, Segoe UI</FontFamily>
    <FontFamily x:Key="AppFontFamilyDisplay">Segoe UI Variable Display, Segoe UI Variable, Segoe UI</FontFamily>
    <FontFamily x:Key="AppFontFamilyText">Segoe UI Variable Text, Segoe UI Variable, Segoe UI</FontFamily>
    <FontFamily x:Key="AppFontFamilySmall">Segoe UI Variable Small, Segoe UI Variable, Segoe UI</FontFamily>

    <!-- Override del recurso de fuente del framework: hace que todos los
         controles (y BaseTextBlockStyle) hereden Segoe UI Variable por defecto. -->
    <FontFamily x:Key="ContentControlThemeFontFamily">Segoe UI Variable Text, Segoe UI Variable, Segoe UI</FontFamily>

    <!-- ===== Tokens no dependientes del tema ===== -->
    <x:Double x:Key="AppCornerRadiusValue">8</x:Double>
    <CornerRadius x:Key="AppCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="AppCardCornerRadius">12</CornerRadius>

    <!-- ===== Profundidad / elevación (Fluent ThemeShadow) =====
         Sombra de elevación compartida por todas las tarjetas (AppCardStyle).
         ThemeShadow proyecta sobre la superficie que tiene detrás (el fondo de
         página) cuando la tarjeta recibe una Translation con componente Z > 0.
         El propio ThemeShadow usa el contenido de la ventana como receptor por
         defecto, por lo que no requiere cablear receptores en cada página.
         Sutil/profesional: Z=16 da una sombra suave de baja elevación. -->
    <media:ThemeShadow x:Key="AppCardShadow" />

    <!-- Densidad compacta (alineada con MainPage: page 12,8 / card 10,7 / spacing 5-6). -->
    <Thickness x:Key="AppSpacingSmall">6</Thickness>
    <Thickness x:Key="AppSpacingMedium">8</Thickness>
    <Thickness x:Key="AppSpacingLarge">10</Thickness>
    <Thickness x:Key="AppPagePadding">12,8</Thickness>

</ResourceDictionary>
